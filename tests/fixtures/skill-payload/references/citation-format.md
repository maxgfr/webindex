# Citation format

Every claim carries the identifier of the source it came from. A sentence with
no identifier is an assertion the reader cannot check, which is the one thing a
grounded report may not contain.
