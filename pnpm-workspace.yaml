allowBuilds:
  esbuild: true

overrides:
  # Patched releases for transitive advisories. esbuild 0.28 is outside tsup's
  # declared ^0.27 range, so check:build also proves the emitted bytes remain stable.
  esbuild: 0.28.1
  nanoid: 3.3.18
